# Isolated unittest suite; desktop, audio hardware and model packages are mocked.
numpy>=1.26.0
rich>=14.0.0
requests>=2.32.0

# Read-only configuration validation
jsonschema>=4.18.0
# Realtime configuration defaults are exercised without network access.
websocket-client>=1.9.0
